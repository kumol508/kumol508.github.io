$();
//dollar sign is a function, the whole document is a function under it

//define a variable
var name = 'Brian';

function say_hi(name){ 
    alert ('hello ' + name)
    //string (something in 'quotes' + variable, need space after the o)
    
}